#include <stdio.h>

signed int     m;

void    v(void)
{
    char    *buffer[520]

    fgets(buffer, 512, stdin);
    prinf(buffer);
    if (m == 64)
    {
        fwrite("Wait what?!\n", 1, 12, stdout);
        system("/bin/sh");
    }
    return;
}

int     main()
{
    v();
    return(0);
}